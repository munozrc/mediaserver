import styles from './styles.module.css'

const Header = () => {
  return (
    <header className={styles.wrapper}>
      <h1 className={styles.logo}>MediaServer</h1>
    </header>
  )
}

export default Header
